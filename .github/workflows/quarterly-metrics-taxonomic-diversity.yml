name: Quarterly Metrics - Taxonomic Diversity

on:
  schedule:
    # 03:45 UTC on the 1st of Mar/Jun/Sep/Dec
    - cron: "45 3 1 3,6,9,12 *"
  workflow_dispatch: {}

concurrency:
  group: quarterly-metrics-taxonomic-diversity
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  generate-metrics:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master

      - uses: r-lib/actions/setup-r@v2

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::rgbif
            any::dplyr
            any::tidyr
            any::jsonlite
            any::httr

      - name: Run taxonomic-diversity metric
        working-directory: metrics
        env:
          GBIF_USER: ${{ secrets.GBIF_USER }}
          GBIF_PWD: ${{ secrets.GBIF_PWD }}
          GBIF_EMAIL: ${{ secrets.GBIF_EMAIL }}
        run: |
          chmod +x run-metrics-pipeline.sh
          ./run-metrics-pipeline.sh --only taxonomic-diversity

      - name: Commit and push updated JSON data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ui/public/data
          if git diff --cached --quiet; then
            echo "No data changes to commit"
          else
            git commit -m "chore: taxonomic-diversity metrics refresh $(date +%Y-%m-%d)"
            for i in 1 2 3 4 5; do
              git pull --rebase --autostash && git push && break
              echo "Push failed, retrying ($i)..."
              sleep $((i * 10))
            done
          fi
